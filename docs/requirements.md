# Privacy Decoy requirements register

This is the canonical normative register as amended on 2026-09-22.
An entry is a design
obligation, **not** evidence that it is implemented. IDs are permanent and
monotonic; superseded entries remain in the register. `MUST`, `SHOULD`, and `MAY`
have their usual normative force. Status `Required` means accepted but generally
unimplemented; `Gate` means evidence is required before the named gate.

Evidence must identify the protected app/version/artifacts, runtime build,
Android/API, OEM/device, ABI, persona, policy, and relevant external dependencies.
The PR 4 (containment), PR 5 (networking), PR 6 (early feasibility decision),
and Later ownership annotations in PD-REQ-001..070 retain their historical
repository research-sequence meaning. The project owner restored the authoritative
46-PR sequence in [canonical-roadmap-1.0.md](canonical-roadmap-1.0.md). Roadmap
numbers remain separate from GitHub PR numbers, and historical variance remains
recorded in the [roadmap reconciliation](roadmap-reconciliation.md). Canonical
Roadmap PR 5 and PR 20 remain mandatory STOP gates; the historical Roadmap PR 6
REDESIGN event retains its PR 6 label and is not represented as having occurred
under PR 5. The source-restoration blocker is closed. Product implementation
remains paused for architecture/feasibility reasons, not because roadmap text is
missing.

negative, adversarial, lifecycle, and failure evidence where applicable.

| ID | Category | Normative requirement | Rationale | Owner / enforcement boundary | Required evidence | Roadmap / status | Notes / limitations |
|---|---|---|---|---|---|---|---|
| PD-REQ-001 | Platform | The product MUST target Android only. | Bound scope. | Product/build | Module and release review | PR 1 / Required | Platform range is provisional. |
| PD-REQ-002 | Platform | Ordinary production operation MUST be root-free and MUST NOT require Magisk, Xposed, LSPosed, or a custom ROM. | Deploy without host compromise. | Runtime/platform | Release build on non-rooted physical devices | PR 6 / Gate | Development tooling is separate. |
| PD-REQ-003 | Network | Privacy Decoy itself MUST NOT use Android VpnService. | Avoid VPN ownership/conflict. | Manifest/network architecture | Manifest, dependency, runtime review | PR 5 / Required | External user VPN remains external. |
| PD-REQ-004 | Supply chain | Production secrets and signing material MUST NOT be committed. | Prevent credential loss. | Release process/repository | Secret scan and custody audit | Later / Required | Also excludes prompts, logs, artifacts. |
| PD-REQ-005 | Claims | User-facing and project documentation MUST NOT claim privacy protection before supporting evidence exists. | Prevent deceptive safety claims. | UX/release review | Claim-to-evidence trace | PR 6 / Gate | Current app provides no protection. |
| PD-REQ-006 | Platform | Ordinary production use MUST NOT depend on ADB. | ADB is not a consumer boundary. | Runtime/platform | Release test with ADB disabled | PR 6 / Gate | Emulators/debuggers MAY be used in development. |
| PD-REQ-007 | Platform | Production MUST NOT require guest root or privileged/system installation. | Preserve ordinary-device model. | Runtime/platform | Non-root physical-device test | PR 6 / Gate | — |
| PD-REQ-008 | Architecture | The preferred design MUST execute imported package artifacts in a controlled runtime; rewriting or re-signing is not preferred. | Preserve artifact identity and reduce mutation. | Import/runtime | Artifact and signature comparison | PR 4 / Gate | Feasibility unknown. |
| PD-REQ-009 | Architecture | Any narrow future rewriting exception MUST have an ADR and explicit user approval. | Make identity/security tradeoff visible. | Import/UX | ADR and consent test | Later / Required | No blanket exception. |
| PD-REQ-010 | Architecture | A full Android VM MUST NOT be chosen merely for implementation convenience. | Limit TCB and resource cost. | Architecture | Decision record | PR 6 / Gate | A demonstrated security need may justify reconsideration. |
| PD-REQ-011 | Isolation | Protected code MUST be treated as adversarial and isolated by process, UID, resources, and authenticated boundaries. | Compatibility is not containment. | Engine/kernel/broker | Adversarial probes | PR 4 / Gate | Includes dynamic/native code. |
| PD-REQ-012 | Isolation | Protected code MUST NOT read or mutate management databases, policy/persona secrets, broker credentials, coverage decisions, or another protected app's state. | Protect the TCB and tenants. | Storage/process/broker | Direct and confused-deputy probes | PR 4 / Gate | Mandatory. |
| PD-REQ-013 | Mediation | Relevant Binder, service, and ContentProvider paths MUST be mediated or blocked. | Prevent framework bypass. | Engine/brokers | Interface inventory and probes | PR 4 / Gate | OEM/hidden interfaces included. |
| PD-REQ-014 | Native | JNI, libc, direct-syscall, `/proc`, `/sys`, filesystem, property, and process observations MUST be analyzed and mandatory leaks blocked. | Java hooks are insufficient. | Engine/kernel boundary | Native probe suite | PR 4 / Gate | Unknown mandatory paths block launch. |
| PD-REQ-015 | Lifecycle | Multiprocess, subprocess, early provider/native initialization, cached handles, stale references, death, restart, and revocation MUST fail safely. | Close timing/lifecycle bypasses. | Supervisor/engine/broker | Race and lifecycle tests | PR 4 / Gate | Protection precedes app code. |
| PD-REQ-016 | Evidence | Hidden/private/OEM API dependencies MUST be inventoried and evidenced per supported platform. | Such dependencies are fragile. | Engine/platform | Release-build OEM matrix | PR 4 / Gate | Absence of evidence is Unknown. |
| PD-REQ-017 | Claims | Privacy Decoy MUST NOT claim total anonymity, universal compatibility, or universal undetectability/fingerprint resistance. | These claims are not supportable. | Product/UX | Release copy review | All / Required | — |
| PD-REQ-018 | Claims | It MUST NOT claim Play Integrity bypass, hardware-backed spoofing, forensic deletion, or deletion of remote history. | Avoid false guarantees. | Product/UX | Release copy review | All / Required | Attestation is never fabricated. |
| PD-REQ-019 | Evidence | Every privacy claim MUST have scoped, reproducible evidence. | Claims require falsifiable support. | Evidence registry | Repeatable test artifact | PR 6 / Gate | Scope fields are listed above. |
| PD-REQ-020 | Coverage | Every relevant capability MUST be classified Fully mediated, Partially mediated, Unsupported, External, N/A, or Unknown. | Expose coverage precisely. | Policy/coverage registry | Complete scoped matrix | PR 6 / Gate | Definitions: full—all paths verified; partial—listed paths only; unsupported—blocked; external—outside control; N/A—not present; unknown—not established. |
| PD-REQ-021 | Coverage | Unknown mandatory coverage MUST block execution before protected code starts; unsupported mandatory paths MUST fail closed. | Never gamble with real data. | Launcher/supervisor | Pre-start negative tests | PR 4 / Gate | No real-value fallback. PD-REQ-088 creates a separately labeled Experimental execution class; it does not satisfy Protected execution. |
| PD-REQ-022 | Persona | A persistent coherent persona, including a default, MUST support explicit inheritance and reassignment. | Avoid cross-surface contradictions. | Persona store/policy | Persistence and assignment tests | Later / Required | Not implemented. |
| PD-REQ-023 | Persona | Identifier scopes MUST be explicit; values MUST be securely generated/derived and stable across restart, reboot, and safe update. | Prevent accidental linkage/change. | Persona cryptography/store | Scope and lifecycle vectors | Later / Required | No hardware-backed fabrication. |
| PD-REQ-024 | Persona | Corruption MUST NOT silently regenerate identity; rotation MUST be transactional. | Prevent incoherence and partial identity. | Persona store | Fault-injection/recovery tests | Later / Required | Fail closed. |
| PD-REQ-025 | Persona | UX/API semantics MUST distinguish edit, reassignment, identifier rotation, persona rotation, clear state, and fresh instance. | Actions have different privacy effects. | UX/persona manager | State-transition tests | Later / Required | Rotation does not erase remote/session history. |
| PD-REQ-026 | Policy | Capability modes MUST be Real, Decoy, Empty, or Deny; Real MUST be controlled mediation rather than bypass. | Make disclosure explicit. | Policy/broker | Mode-by-mode access tests | Later / Required | Sensitive defaults favor Decoy/Empty/Deny. |
| PD-REQ-027 | Policy | Policy transition and revocation MUST invalidate capabilities and fail safely. | Stop stale access. | Policy/broker/supervisor | Concurrent revocation tests | PR 4 / Gate | Includes cached handles. |
| PD-REQ-028 | Persona | Device/Custom Device selection MUST produce coherent device, locale, region, telephony, local-network, and stable plausible location state. | Reduce contradictions. | Persona/policy | Cross-surface consistency tests | Later / Required | “Plausible” is not “undetectable.” |
| PD-REQ-029 | Location | Host GPS MUST NOT silently become fallback; fused, framework, Play Services, and other location surfaces MUST be explicitly covered or blocked. | Prevent real-location disclosure. | Location brokers/engine | Surface-specific probes | Later / Required | Unsupported paths fail closed. |
| PD-REQ-030 | Packages | Protected apps MUST observe an explicit virtual package universe; persona sharing MUST NOT imply app visibility. | Prevent host inventory leakage. | Package manager engine | Query/intent/provider probes | PR 4 / Gate | — |
| PD-REQ-031 | Host services | Relevant Settings, Play Services, telephony, location, provider, and external Binder paths MUST be mediated or blocked. | Host services can reveal real state. | Engine/brokers | Service inventory and probes | PR 4 / Gate | Availability varies by device. |
| PD-REQ-032 | Network | All traffic-producing protected processes, helpers, and brokers MUST be identified and attributable. | Unknown senders evade policy. | Network/supervisor | Process and packet correlation | PR 5 / Gate | Includes background/native traffic. |
| PD-REQ-033 | Network | “Require VPN for protected apps” MUST default ON; unverifiable required routing MUST block networking rather than use a physical fallback. | Fail closed on route uncertainty. | Network gate | Default-policy and route-change/failure tests | PR 5 / Gate | VPN presence alone is not proof of a trusted/full tunnel. |
| PD-REQ-034 | Network | IPv4, IPv6, DNS, TCP, UDP, QUIC, Java, native, background, and helper paths MUST have independent evidence. | Coverage differs by stack/path. | Network gate/test observer | Independent packet captures | PR 5 / Gate | External VPN is outside the TCB unless explicitly assessed. |
| PD-REQ-035 | Sensors | Supported synthetic sensor behavior MUST be coherent; unsupported sensor surfaces MUST be denied. | Avoid real readings and contradictions. | Sensor brokers | Physical and virtual sensor probes | Later / Required | Silent passthrough forbidden. |
| PD-REQ-036 | Personal data | Contacts, calendar, call logs, SMS, media, clipboard, and similar personal datasets SHOULD default Empty. | Minimize disclosure. | Dataset brokers/policy | Dataset access tests | Later / Required | Real requires controlled mediation. |
| PD-REQ-037 | Hardware/auth | Camera, microphone, Bluetooth, NFC, USB, accounts, Credential Manager, passkeys, biometrics, Keystore, and attestation MUST have explicit policy. | High-risk capabilities cannot be implicit. | Policy/brokers/platform | Per-capability matrix | Later / Required | Deny/unsupported acceptable initially; never fabricate hardware attestation. |
| PD-REQ-038 | Import | Import MUST validate complete base/split/APKS sets, signing lineage, integrity, and atomic staging. | Prevent malformed or mixed artifacts. | Importer | Positive/malformed fixtures | Later / Required | No runtime implementation in PR 3. |
| PD-REQ-039 | Import | External runtime state MUST NOT be imported; Play Store inside Decoy is not required for 1.0. | Avoid importing secrets/complexity. | Importer | State inspection | Later / Required | — |
| PD-REQ-040 | Updates | An outside-installed app MAY supply update artifacts only if safe validation and isolation are demonstrated. | Avoid trusting host installation state. | Updater/importer | Artifact provenance tests | Later / Required | Not a runtime dependency. |
| PD-REQ-041 | Storage | Protected storage MUST be isolated across host and protected apps, including native filesystem access; arbitrary host shared-storage passthrough is forbidden. | Prevent file disclosure. | Engine/storage/kernel | Java/native traversal probes | PR 4 / Gate | Explicit broker grants only. |
| PD-REQ-042 | Storage | Personas, policy, ledger, credentials, and sensitive state MUST be excluded from backup/transfer until validated. | Prevent out-of-boundary copies. | Storage/backup | OEM backup/transfer tests | Later / Required | Existing exclusions are not universal proof. |
| PD-REQ-043 | Removal | Removal MUST revoke grants/keys and clean logical state without claiming forensic erasure. | Honest deletion semantics. | Storage/key manager | State/key inspection | Later / Required | Remote state remains. |
| PD-REQ-044 | Launch | All mandatory validation MUST complete before any protected code, provider, or native initializer runs. | No pre-policy window. | Launcher/supervisor | Instrumented cold-start probes | PR 4 / Gate | Failure blocks launch. |
| PD-REQ-045 | Supervision | Supervisor health, authentication, recovery, and revocation MUST be fail-safe with bounded retries and no busy loop/restart storm. | Keep boundary dependable. | Supervisor | Fault/death/load tests | PR 4 / Gate | — |
| PD-REQ-046 | Entry routes | Launcher, shortcut, deep link, share, job, alarm, service, broadcast, push, notification action, and background entry MUST eventually be covered or blocked. | Alternate entry must not bypass validation. | Dispatcher/supervisor | Route matrix | Later / Required | Initial support may be narrow and explicit. |
| PD-REQ-047 | UX integration | Notifications and sharing MUST preserve originating protected-app identity where appropriate while visibly identifying Privacy Decoy. | Avoid source confusion. | UI/brokers | UX and spoofing tests | Later / Required | — |
| PD-REQ-048 | Boundary | Browser, authentication, share, keyboard, autofill, accessibility, and screen-capture handoffs MUST be explicit and documented. | These cross trust boundaries. | UX/policy | Handoff matrix and tests | Later / Required | External means not protected by Decoy. |
| PD-REQ-049 | UX safety | UI MUST clearly distinguish blocked, unsupported, unknown, and error states and protected versus outside-installed copies. | Users need actionable truth. | UX | Usability and copy review | Later / Required | Must explain remote-history limits. |
| PD-REQ-050 | App update | Protected-app updates MUST default to Ask and validate replacement, signature/lineage, splits, integrity, and coverage. | Updates can alter attack surface. | Updater/coverage | Upgrade fixture matrix | Later / Required | New versions may become unsupported. |
| PD-REQ-051 | App update | Update failure MUST preserve old package/state and MUST NOT reduce privacy; rollback MAY occur only when data-safe. | Compatibility never overrides privacy. | Updater/storage | Atomicity/rollback tests | Later / Required | — |
| PD-REQ-052 | Self-update | Self-update discovery MUST use published GitHub Releases only and ignore commits, branches, PRs, tag-only objects, drafts, and prereleases. | Define trusted release channel. | Updater | API fixture tests | Later / Required | Direct/GitHub distribution assumption. |
| PD-REQ-053 | Self-update | Update checks MUST NOT transmit persona/tracking data. | Minimize metadata. | Updater/network | Request inspection | Later / Required | — |
| PD-REQ-054 | Ledger | Privacy Access Ledger MUST be local, bounded, clearable, and metadata-only. | Accountability without a new sensitive corpus. | Ledger/store | Schema, bound, clear tests | Later / Required | No payloads, precise host data, or raw sensitive exceptions. |
| PD-REQ-055 | Ledger | Ledger failure MUST NOT open access, and ledger silence MUST NOT be represented as proof of non-access. | Logging is not enforcement. | Broker/ledger/UX | Failure injection | Later / Required | — |
| PD-REQ-056 | Resources | Queues, retries, caches, logs, and retained evidence MUST be bounded. | Resist exhaustion/privacy accumulation. | Every subsystem | Load/resource tests | Later / Required | No busy loops. |
| PD-REQ-057 | Testing | Validation MUST include positive, negative, adversarial, lifecycle, and failure tests using controlled probe apps and safe fixtures. | Ordinary apps are inadequate probes. | Test program | Reproducible suites | PR 4–6 / Gate | No real accounts/private data before gate. |
| PD-REQ-058 | Testing | Network claims MUST use independent packet observation; storage claims MUST use persistent-state observation; release claims MUST include release builds on non-rooted devices. | Avoid self-report circularity. | Independent observer | Captures/state snapshots/device records | PR 5–6 / Gate | Emulator-only evidence is insufficient. |
| PD-REQ-059 | Review | A later independent Android and native security review MUST precede production privacy claims. | TCB requires specialist scrutiny. | Project governance | Review report and remediation | Later / Required | Not replaced by internal tests. |
| PD-REQ-060 | Supply chain | Any engine MUST sit behind a documented replacement boundary with dependency, license, provenance, and vulnerability inventories. | Contain third-party and migration risk. | Engine adapter/build | SBOM/source/license audit | PR 4–6 / Gate | Runtime capability must not leak into management domain. |
| PD-REQ-061 | Distribution | Distribution-channel constraints and production-signing custody MUST be documented and tested; signing secrets MUST never enter git, prompts, logs, or artifacts. | Preserve authenticity. | Release process | Channel/policy and custody audit | Later / Required | Google Play feasibility remains open. |
| PD-REQ-062 | Repository | The clean-root rule MUST be preserved: Android material belongs under `android/`, project documentation under `docs/`, and new root files must be genuinely repository-wide. | Maintain navigable ownership. | Repository review | Tree/diff inspection | All / Required | Established by PR 2. |
| PD-REQ-063 | Feasibility | PR 6 MUST be a user decision STOP gate; work MUST stop, redesign, or mark unsupported when mandatory guarantees lack credible evidence. | Prevent momentum from becoming a false guarantee. | Governance | Signed decision against PR 4/5 evidence | PR 6 / Gate | No production engine is selected by this register. |
| PD-REQ-064 | Device selection | Device and Custom Device MUST be the only selection modes; there MUST NOT be an Automatic device or persona selection mode. | Keep identity choice explicit and reviewable. | Persona/UX | Selection-state and migration tests | Later / Required | Device means a validated catalog profile; Custom Device is explicit user configuration. |
| PD-REQ-065 | Persona isolation | Assigning apps to one Decoy Persona MUST NOT automatically share protected-app storage, login/session state, package visibility, or every identifier namespace/scope. | A persona is not an application sandbox or account. | Persona/engine/storage | Cross-app isolation and identifier-scope tests | Later / Required | Any intentional sharing requires an explicit scoped design and policy. |
| PD-REQ-066 | Rotation UX | Persona and identifier rotation MUST be transactional and MUST show the affected scope and impact before commitment. | Prevent accidental partial or over-broad identity change. | Persona store/UX | Preview, commit, rollback and fault-injection tests | Later / Required | Rotation does not erase server-side, cookie, session, or account history. |
| PD-REQ-067 | Policy reset | Per-category policy overrides MUST be explicit and MUST provide a safe reset-to-persona operation; reset, revocation, and transitions MUST fail closed. | Avoid invisible policy drift and unsafe reset behavior. | Policy/UX/brokers | Override/reset and concurrent revocation tests | Later / Required | Reset restores persona policy, not uncontrolled passthrough. |
| PD-REQ-068 | Device catalog | The device-profile catalog MUST contain only validated profiles and MUST NOT advertise unsupported spoofing; the initial catalog SHOULD remain deliberately small. | Catalog size is not evidence of coherent mediation. | Catalog/evidence/UX | Profile provenance and cross-surface validation | Later / Required | Approximately 10 manufacturers/100 profiles is a long-term planning goal, not current support. |
| PD-REQ-069 | Location | Offline/local geocoding SHOULD be preferred where practical for decoy-location supporting data. | Reduce network disclosure and availability dependency. | Location data/broker | Network-observation and offline behavior tests | Later / Required | Remote data use requires explicit threat and privacy analysis. |
| PD-REQ-070 | Network scope | Privacy Decoy 1.0 MUST NOT add a built-in tracker blocklist or VPN system merely to duplicate the user's external VPN. | Avoid duplicate scope and VPN ownership. | Product/network architecture | Feature, dependency and manifest review | PR 5 / Required | This does not weaken VPN-default, route-verification, attribution, or fail-closed requirements. |

| PD-REQ-071 | Advertising identifiers | Advertising and ad-tech identifiers exposed to protected apps MUST be explicitly scoped and mediated across platform advertising identifiers and equivalent framework, Play Services, SDK, and library paths; there MUST be no silent host-real fallback, Real MUST remain controlled mediation, Decoy MUST follow explicit scope, and Empty/Deny MUST be available where supported. | Prevent tracking leakage and unintended correlation. | Identifier brokers/policy | Framework/SDK/library path, scope, reset, rotation, and limited-advertising tests | Later / Required | Reset, rotation, and limited-advertising semantics must remain persona-consistent; sharing a persona MUST NOT by itself share identifiers across apps. |
| PD-REQ-072 | Battery/power | Battery, charging, and power observations exposed to protected apps MUST be deliberately mediated, synthetic, safely absent, or denied, never silently backed by real host state. | Prevent host-state leakage and contradictions. | Power brokers/engine | Temporal and cross-path framework, broadcast, service, and native probes | Later / Required | Decoy percentage, charge, plug, and power states must be mutually plausible; mandatory unsupported paths fail closed or remain Unsupported/Unknown under PD-REQ-020/021. |
| PD-REQ-073 | Android identity | Reported persona Android/build/device descriptors MUST remain distinct from the actual runtime's API behavior, supported API level, ABI, kernel/framework capabilities, platform features, and containment capabilities; synthetic descriptions MUST NOT support claims for capabilities the actual runtime lacks. | Prevent descriptive spoofing from becoming a false support claim. | Persona/coverage/UX | Descriptor-to-runtime capability matrix and claim review | Later / Required | Coverage, UI, and evidence must preserve the distinction. |
| PD-REQ-074 | Network persona | Supported network-persona metadata MUST remain coherent with locale, region, location, carrier/SIM, and local-network state; local spoofing MUST NOT be described as changing the public IP. Any external exit-IP/geolocation lookup MUST be explicitly user-authorized, minimized, approximate when geography is shown, nonessential to protected execution, and never silently contact a third party or guess unavailable geography. | Prevent correlation and undisclosed network disclosure. | Persona/network/UX | Cross-surface tests and independent request/absence observation | Later / Required | Unknown geography remains Unknown; the external VPN remains external unless separately assessed. |
| PD-REQ-075 | Sensors | Supported synthetic/dynamic sensor observations MUST demonstrate temporal plausibility, expected stability, coherent rates/ranges/units, consistency across supported Java/framework/native/SDK paths and device capability declarations, and coherent pause/resume/restart/rotation behavior. | Prevent real readings and cross-surface contradictions. | Sensor brokers/engine | Cross-API temporal, lifecycle, and rotation probes | Later / Required | Unsupported paths MUST be denied or Unsupported, never host passthrough. Complements PD-REQ-035. |
| PD-REQ-076 | Personal data | Android permission state MUST NOT by itself authorize real host personal-data disclosure; granted permissions for contacts, calendar, messages, call history, media, accounts, clipboard, and similar data still require explicit controlled Real mediation or deliberate Decoy/Empty/Deny policy. | Permission is not a privacy-boundary decision. | Dataset brokers/policy/storage | Permission-by-policy and absent/Unknown-policy tests | Later / Required | Absent policy or Unknown mandatory coverage MUST NOT expose host data; complements PD-REQ-036 and PD-REQ-041. |
| PD-REQ-077 | Persona templates | Persistent/reusable persona templates, if supported, MUST define explicit versioning, persistence, assignment, controlled update/migration, and transactional failure semantics while preserving identifier scope and package-visibility and storage/session isolation. | Keep reusable identity policy from becoming shared app state. | Persona store/UX | Version, assignment, migration, isolation, and fault tests | Later / Required | A template is configuration/identity policy, not shared storage or login/session state; automatic selection MUST NOT be invented and PD-REQ-064 controls selection. |
| PD-REQ-078 | Diagnostics | Developer Mode diagnostics MUST remain separate from the Privacy Access Ledger and MUST NOT emit, record, or retain raw protected/persona/host values; diagnostics MUST use only bounded, redacted, allowlisted metadata/categories/status evidence, identify evidence scope and Unknown/Unsupported/Partial states, MUST NOT weaken policy or mediation, and MUST NOT convert missing evidence into success. | Prevent diagnostics from becoming a leak or false proof. | Diagnostics/ledger/UX | Separation, redaction, bounds, failure, and state-label tests | Later / Required | The Ledger remains local, bounded, clearable, redacted, metadata-only mediation metadata and not proof of non-access; missing evidence never becomes success. |
| PD-REQ-079 | Camera/media | Synthetic camera/media input MUST remain deferred beyond Privacy Decoy 1.0 unless a later canonical decision explicitly reprioritizes it; in 1.0, physical capture paths MUST still be explicitly mediated, denied, or Unsupported. | Avoid unsupported media claims and physical-input leaks. | Hardware/media policy | Physical-path and claim review | 1.0 / Required | Unknown mandatory paths cannot expose physical input; blocking or absence is not synthetic-media support and PD-REQ-037 is not weakened. |
| PD-REQ-080 | Product scope | Privacy Decoy MUST NOT adopt features solely because competitors provide them where they conflict with the canonical architecture, including root/guest-root, Magisk/Xposed/LSPosed/custom-ROM dependence, patched-kernel dependence, routine APK rewriting/re-signing, cosmetic cloning as privacy mediation, a convenience full guest Android, or a duplicate built-in tracker/VPN system. | Prevent competitive-feature drift from weakening the boundary. | Product governance/architecture | Scope and architecture review | All / Required | Any future exception remains subject to applicable ADR and explicit user-decision rules. |
| PD-REQ-081 | Path coverage | Capability coverage MUST include material Play Services, embedded third-party SDK, WebView/browser-mediated, JNI/native-library, dynamically loaded code, and alternate library-wrapper paths where relevant. | Direct framework mediation does not establish wrapper-path safety. | Coverage registry/engine | Path inventory and adversarial per-path probes | Later / Gate | An SDK/library path cannot be presumed safe from direct-API results; Unknown mandatory paths block execution. |
| PD-REQ-082 | Testing | Every supported synthetic value/capability MUST be validated, as applicable, for stability, identifier scope, restart/reboot/safe-update persistence, cross-API consistency, temporal plausibility, coherent rotation, safe failure, adversarial bypass resistance, enforcement/privacy behavior, and compatibility within its supported scope. | Establish both privacy enforcement and usable bounded support. | Test program/evidence registry | Scoped adversarial and compatibility suites on required matrices | 1.0 / Gate | Emulator/debug evidence alone is insufficient; physical non-rooted devices and release-equivalent builds remain required for production claims. |
| PD-REQ-083 | Acceptance | Privacy Decoy 1.0 protection/release claims MUST pass a deliberate acceptance gate: no mandatory Unknown coverage; fail-closed Unsupported paths; protection before code; evidenced persona stability/scope/coherence; scoped native/framework/SDK/library, storage/personal-data, and independent network/VPN/fallback evidence; physical non-rooted release-equivalent support-matrix evidence; enforcement and compatibility tests; and completed independent Android/native security review. | Prevent incomplete evidence from becoming a release claim. | Release governance | Completed acceptance checklist and evidence trace | 1.0 / Gate | No unresolved mandatory requirement may be hidden by compatibility success; Experimental execution cannot satisfy this Protected-claim gate; a green debug/emulator suite alone is insufficient. |
| PD-REQ-084 | Governance | Privacy Decoy 1.0 MUST be governed by the canonical 46-PR roadmap, with roadmap numbering separate from GitHub PR numbering and mandatory STOP/decision gates at Roadmap PR 5 and PR 20; gates MUST NOT be silently moved, removed, or inferred satisfied from GitHub numbering. Historical labels MUST remain accurate, and no later implementation may proceed while the authoritative sequence is missing or materially unreconciled. | Preserve canonical sequencing without rewriting history. | Project governance | Restored roadmap source, reconciliation review, and gate decisions | Roadmap / Gate | Historical Roadmap PR 6 REDESIGN remains labeled PR 6; the variance is documented. Roadmap PR 20 has not occurred. |
| PD-REQ-085 | Repository privacy | Privacy Decoy repositories, pull requests, Actions logs, retained CI artifacts, diagnostic evidence intended for project review, and other project publication surfaces MUST be treated as public for secrecy purposes regardless of actual repository visibility and MUST NOT contain secrets, credentials, private user data, raw protected/persona/host values, or production signing material. | Prevent publication of private or sensitive material. | Repository/review/CI/diagnostics | Diff/tree review, secret scanning where applicable, log/artifact inspection, and diagnostic redaction tests | All / Required | Actual GitHub visibility never relaxes this rule; controlled synthetic fixtures are allowed; private user data is never required for research or CI evidence. |


| PD-REQ-086 | Admission analysis | Every imported application artifact set MUST receive a pre-execution admission analysis covering the complete known base/split set, executable DEX sources, declared components/processes, native-library inventory, and material dynamic/SDK/runtime capabilities before Protected execution is authorized. Admission analysis is evidence, not proof by omission. | Establish the scoped surface before execution. | Admission engine | Analyzer fixtures and artifact inventories | AG-1 / Gate | Unknown remains Unknown. |
| PD-REQ-087 | Protected eligibility | Protected Mode MUST remain subject to PD-REQ-021: mandatory Unknown coverage blocks Protected execution. An application version is Protected-eligible only within a precisely evidenced execution scope. Presence of app-controlled native code, opaque executable content, or an unmediated dynamic path MUST NOT be presumed safe. | Preserve fail-closed protection. | Admission/runtime | Scoped adversarial evidence | AG-1 / Gate | Hook availability is not containment evidence. |
| PD-REQ-088 | Experimental execution | Privacy Decoy MAY offer `Experimental & Unproven Compatibility Mode` for an application/artifact set whose blocking result is Unknown/unproven coverage only, provided no mandatory bypass has been positively established and the app is not incompatible. Experimental execution MUST NOT be represented as Protected execution or as a privacy guarantee. Explicit informed user opt-in is required. | Permit honest bounded compatibility research. | Admission/UX/runtime | Classification and consent tests | AG-1 / Gate | This is not an exception to PD-REQ-021. |
| PD-REQ-089 | Experimental consent | Experimental consent MUST be scoped to the exact application version and admitted artifact/split set, MUST NOT be global or automatically inherited by updates, and MUST be invalidated by relevant application artifact changes or re-analysis. | Prevent stale or blanket consent. | Registry/UX | Version, split, update, and re-analysis tests | AG-1 / Gate | Consent follows an immutable admission generation. |
| PD-REQ-090 | Hard stops | A positively identified mandatory bypass/known-unsafe result MUST NOT offer Experimental execution. A fundamentally incompatible application MUST NOT offer Experimental execution. These states require a hard stop. | Unknown is distinct from known unsafe and incompatible. | Launch gateway | Negative classification/launch tests | AG-1 / Gate | No override checkbox. |
| PD-REQ-091 | Runtime executable-code control | Protected execution MUST detect and block or classify newly introduced executable DEX/APK/JAR/native content before use wherever the runtime can enforce such a boundary. Unobservable or opaque executable introduction is Unknown and cannot silently remain Protected. A positive known-unsafe discovery during execution MUST terminate or revoke the affected execution safely. | Preserve admission after launch. | Runtime/supervisor | Dynamic-code and termination probes | AG-1 / Gate | The boundary is not yet proven. |
| PD-REQ-092 | Experimental data minimization | Experimental execution MUST apply all available PD mediation and SHOULD use Decoy, Empty, or Deny for sensitive host personal data by default. Experimental status MUST NOT itself authorize Real host data, disable VPN requirements, enable host-GPS fallback, or weaken unrelated product invariants. Unproven paths remain explicitly unproven. | Limit exposure without making a guarantee. | Policy/runtime | Defaults and invariant tests | AG-1 / Gate | External-VPN requirements remain applicable. |
| PD-REQ-093 | Admission honesty | Static analysis, SDK detection, ELF/import scanning, compatibility success, or absence of a detected dangerous pattern MUST NOT by itself be treated as proof that an application is fully mediated. Packed, encrypted, dynamically generated, downloaded, or otherwise opaque executable behavior MUST remain Unknown unless positively evidenced. No numerical “safety score” may substitute for capability-level coverage. | Avoid false confidence. | Admission/coverage | Opaque and negative fixtures | AG-1 / Gate | Detection absence is not safety proof. |
| PD-REQ-094 | Third-party reuse | External open-source virtualization, privacy, instrumentation, Binder, and native-hook projects MAY inform PD design, but production reuse MUST require exact-source identification, license/provenance review, dependency/native-binary review, security review proportional to TCB role, and an explicit integration decision. A permissive repository root license MUST NOT be treated as proof of every bundled/inherited component. Opaque security-critical binaries MUST NOT enter PD's trusted computing base without resolved source/provenance. | Govern supply-chain trust. | Architecture/supply chain | Audit and integration decision | Before reuse / Gate | Reference status is not approval. |
| PD-REQ-095 | Claim separation | Experimental & Unproven execution MUST remain visibly distinguishable from Protected Mode in launch UI, runtime indication, coverage reporting, support documentation, Ledger/diagnostic metadata where applicable, and release claims. Experimental compatibility MUST NOT count toward Protected-mode acceptance or evidence. | Prevent experimental compatibility from becoming a protection claim. | UX/coverage/release | End-to-end labeling and claim review | AG-1 and 1.0 / Gate | Persistent distinction is mandatory. |

## Traceability and handoff

Each implementation change must cite requirement IDs and link tests and evidence.
In the **historical repository execution context**, PR 4 was assigned to answer
execution without installation/re-signing; actual process/UID and
management boundaries; Binder/service/provider/package/host leakage; JNI/syscall,
`/proc`, `/sys`, filesystem/property leakage; early initialization; multiprocess;
stale capabilities; and pre-code fail-closed launch. Historical PR 5 was assigned
to identify every
traffic producer and establish attribution, external-VPN route verification,
route-change behavior, fail-closed networking, protocol/path coverage, and
independent packet evidence. Historical Roadmap PR 6 reviewed those results and
recorded the explicit user decision **A — REDESIGN**; compatibility was not proof
of feasibility.

This historical PR 4/5/6 research sequence remains valid scoped evidence/history;
it does not replace the restored canonical 46-PR roadmap. Canonical Roadmap PR 5
and Roadmap PR 20 remain mandatory STOP gates. Historical Roadmap PR 6 REDESIGN
is not renamed. The roadmap source and amendment refinements are restored; future work must
follow that canonical governance. Product implementation remains paused pending a
separate architecture/feasibility decision; see [roadmap
reconciliation](roadmap-reconciliation.md).
