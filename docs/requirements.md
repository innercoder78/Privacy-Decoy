# Privacy Decoy requirements register

This is the canonical normative register as of 2026-09-19. An entry is a design
obligation, **not** evidence that it is implemented. IDs are permanent and
monotonic; superseded entries remain in the register. `MUST`, `SHOULD`, and `MAY`
have their usual normative force. Status `Required` means accepted but generally
unimplemented; `Gate` means evidence is required before the named gate.

Evidence must identify the protected app/version/artifacts, runtime build,
Android/API, OEM/device, ABI, persona, policy, and relevant external dependencies.
Roadmap ownership is PR 4 (containment), PR 5 (networking), PR 6 (mandatory early
feasibility decision), or Later. “Test” below means reproducible positive,
negative, adversarial, lifecycle, and failure evidence where applicable.

| ID | Category | Normative requirement | Rationale | Owner / enforcement boundary | Required evidence | Roadmap / status | Notes / limitations |
|---|---|---|---|---|---|---|---|
| PD-REQ-001 | Platform | The product MUST target Android only. | Bound scope. | Product/build | Module and release review | PR 1 / Required | Platform range is provisional. |
| PD-REQ-002 | Platform | Ordinary production operation MUST be root-free and MUST NOT require Magisk, Xposed, LSPosed, or a custom ROM. | Deploy without host compromise. | Runtime/platform | Release build on non-rooted physical devices | PR 6 / Gate | Development tooling is separate. |
| PD-REQ-003 | Network | Privacy Decoy itself MUST NOT use Android VpnService. | Avoid VPN ownership/conflict. | Manifest/network architecture | Manifest, dependency, runtime review | PR 5 / Required | External user VPN remains external. |
| PD-REQ-004 | Supply chain | Production secrets and signing material MUST NOT be committed. | Prevent credential loss. | Release process/repository | Secret scan and custody audit | Later / Required | Also excludes prompts, logs, artifacts. |
| PD-REQ-005 | Claims | User-facing and project documentation MUST NOT claim privacy protection before supporting evidence exists. | Prevent deceptive safety claims. | UX/release review | Claim-to-evidence trace | PR 6 / Gate | Current app provides no protection. |
| PD-REQ-006 | Platform | Ordinary production use MUST NOT depend on ADB. | ADB is not a consumer boundary. | Runtime/platform | Release test with ADB disabled | PR 6 / Gate | Emulators/debuggers MAY be used in development. |
| PD-REQ-007 | Platform | Production MUST NOT require guest root or privileged/system installation. | Preserve ordinary-device model. | Runtime/platform | Non-root physical-device test | PR 6 / Gate | — |
| PD-REQ-008 | Architecture | The preferred design MUST execute imported package artifacts in a controlled runtime; rewriting or re-signing is not preferred. | Preserve artifact identity and reduce mutation. | Import/runtime | Artifact and signature comparison | PR 4 / Gate | Feasibility unknown. |
| PD-REQ-009 | Architecture | Any narrow future rewriting exception MUST have an ADR and explicit user approval. | Make identity/security tradeoff visible. | Import/UX | ADR and consent test | Later / Required | No blanket exception. |
| PD-REQ-010 | Architecture | A full Android VM MUST NOT be chosen merely for implementation convenience. | Limit TCB and resource cost. | Architecture | Decision record | PR 6 / Gate | A demonstrated security need may justify reconsideration. |
| PD-REQ-011 | Isolation | Protected code MUST be treated as adversarial and isolated by process, UID, resources, and authenticated boundaries. | Compatibility is not containment. | Engine/kernel/broker | Adversarial probes | PR 4 / Gate | Includes dynamic/native code. |
| PD-REQ-012 | Isolation | Protected code MUST NOT read or mutate management databases, policy/persona secrets, broker credentials, coverage decisions, or another protected app's state. | Protect the TCB and tenants. | Storage/process/broker | Direct and confused-deputy probes | PR 4 / Gate | Mandatory. |
| PD-REQ-013 | Mediation | Relevant Binder, service, and ContentProvider paths MUST be mediated or blocked. | Prevent framework bypass. | Engine/brokers | Interface inventory and probes | PR 4 / Gate | OEM/hidden interfaces included. |
| PD-REQ-014 | Native | JNI, libc, direct-syscall, `/proc`, `/sys`, filesystem, property, and process observations MUST be analyzed and mandatory leaks blocked. | Java hooks are insufficient. | Engine/kernel boundary | Native probe suite | PR 4 / Gate | Unknown mandatory paths block launch. |
| PD-REQ-015 | Lifecycle | Multiprocess, subprocess, early provider/native initialization, cached handles, stale references, death, restart, and revocation MUST fail safely. | Close timing/lifecycle bypasses. | Supervisor/engine/broker | Race and lifecycle tests | PR 4 / Gate | Protection precedes app code. |
| PD-REQ-016 | Evidence | Hidden/private/OEM API dependencies MUST be inventoried and evidenced per supported platform. | Such dependencies are fragile. | Engine/platform | Release-build OEM matrix | PR 4 / Gate | Absence of evidence is Unknown. |
| PD-REQ-017 | Claims | Privacy Decoy MUST NOT claim total anonymity, universal compatibility, or universal undetectability/fingerprint resistance. | These claims are not supportable. | Product/UX | Release copy review | All / Required | — |
| PD-REQ-018 | Claims | It MUST NOT claim Play Integrity bypass, hardware-backed spoofing, forensic deletion, or deletion of remote history. | Avoid false guarantees. | Product/UX | Release copy review | All / Required | Attestation is never fabricated. |
| PD-REQ-019 | Evidence | Every privacy claim MUST have scoped, reproducible evidence. | Claims require falsifiable support. | Evidence registry | Repeatable test artifact | PR 6 / Gate | Scope fields are listed above. |
| PD-REQ-020 | Coverage | Every relevant capability MUST be classified Fully mediated, Partially mediated, Unsupported, External, N/A, or Unknown. | Expose coverage precisely. | Policy/coverage registry | Complete scoped matrix | PR 6 / Gate | Definitions: full—all paths verified; partial—listed paths only; unsupported—blocked; external—outside control; N/A—not present; unknown—not established. |
| PD-REQ-021 | Coverage | Unknown mandatory coverage MUST block execution before protected code starts; unsupported mandatory paths MUST fail closed. | Never gamble with real data. | Launcher/supervisor | Pre-start negative tests | PR 4 / Gate | No real-value fallback. |
| PD-REQ-022 | Persona | A persistent coherent persona, including a default, MUST support explicit inheritance and reassignment. | Avoid cross-surface contradictions. | Persona store/policy | Persistence and assignment tests | Later / Required | Not implemented. |
| PD-REQ-023 | Persona | Identifier scopes MUST be explicit; values MUST be securely generated/derived and stable across restart, reboot, and safe update. | Prevent accidental linkage/change. | Persona cryptography/store | Scope and lifecycle vectors | Later / Required | No hardware-backed fabrication. |
| PD-REQ-024 | Persona | Corruption MUST NOT silently regenerate identity; rotation MUST be transactional. | Prevent incoherence and partial identity. | Persona store | Fault-injection/recovery tests | Later / Required | Fail closed. |
| PD-REQ-025 | Persona | UX/API semantics MUST distinguish edit, reassignment, identifier rotation, persona rotation, clear state, and fresh instance. | Actions have different privacy effects. | UX/persona manager | State-transition tests | Later / Required | Rotation does not erase remote/session history. |
| PD-REQ-026 | Policy | Capability modes MUST be Real, Decoy, Empty, or Deny; Real MUST be controlled mediation rather than bypass. | Make disclosure explicit. | Policy/broker | Mode-by-mode access tests | Later / Required | Sensitive defaults favor Decoy/Empty/Deny. |
| PD-REQ-027 | Policy | Policy transition and revocation MUST invalidate capabilities and fail safely. | Stop stale access. | Policy/broker/supervisor | Concurrent revocation tests | PR 4 / Gate | Includes cached handles. |
| PD-REQ-028 | Persona | Device/Custom Device selection MUST produce coherent device, locale, region, telephony, local-network, and stable plausible location state. | Reduce contradictions. | Persona/policy | Cross-surface consistency tests | Later / Required | “Plausible” is not “undetectable.” |
| PD-REQ-029 | Location | Host GPS MUST NOT silently become fallback; fused, framework, Play Services, and other location surfaces MUST be explicitly covered or blocked. | Prevent real-location disclosure. | Location brokers/engine | Surface-specific probes | Later / Required | Unsupported paths fail closed. |
| PD-REQ-030 | Packages | Protected apps MUST observe an explicit virtual package universe; persona sharing MUST NOT imply app visibility. | Prevent host inventory leakage. | Package manager engine | Query/intent/provider probes | PR 4 / Gate | — |
| PD-REQ-031 | Host services | Relevant Settings, Play Services, telephony, location, provider, and external Binder paths MUST be mediated or blocked. | Host services can reveal real state. | Engine/brokers | Service inventory and probes | PR 4 / Gate | Availability varies by device. |
| PD-REQ-032 | Network | All traffic-producing protected processes, helpers, and brokers MUST be identified and attributable. | Unknown senders evade policy. | Network/supervisor | Process and packet correlation | PR 5 / Gate | Includes background/native traffic. |
| PD-REQ-033 | Network | “Require VPN for protected apps” SHOULD default on; unverifiable required routing MUST block networking rather than use a physical fallback. | Fail closed on route uncertainty. | Network gate | Route-change/failure tests | PR 5 / Gate | VPN presence alone is not routing proof. |
| PD-REQ-034 | Network | IPv4, IPv6, DNS, TCP, UDP, QUIC, Java, native, background, and helper paths MUST have independent evidence. | Coverage differs by stack/path. | Network gate/test observer | Independent packet captures | PR 5 / Gate | External VPN is outside the TCB unless explicitly assessed. |
| PD-REQ-035 | Sensors | Supported synthetic sensor behavior MUST be coherent; unsupported sensor surfaces MUST be denied. | Avoid real readings and contradictions. | Sensor brokers | Physical and virtual sensor probes | Later / Required | Silent passthrough forbidden. |
| PD-REQ-036 | Personal data | Contacts, calendar, call logs, SMS, media, clipboard, and similar personal datasets SHOULD default Empty. | Minimize disclosure. | Dataset brokers/policy | Dataset access tests | Later / Required | Real requires controlled mediation. |
| PD-REQ-037 | Hardware/auth | Camera, microphone, Bluetooth, NFC, USB, accounts, Credential Manager, passkeys, biometrics, Keystore, and attestation MUST have explicit policy. | High-risk capabilities cannot be implicit. | Policy/brokers/platform | Per-capability matrix | Later / Required | Deny/unsupported acceptable initially; never fabricate hardware attestation. |
| PD-REQ-038 | Import | Import MUST validate complete base/split/APKS sets, signing lineage, integrity, and atomic staging. | Prevent malformed or mixed artifacts. | Importer | Positive/malformed fixtures | Later / Required | No runtime implementation in PR 3. |
| PD-REQ-039 | Import | External runtime state MUST NOT be imported; Play Store inside Decoy is not required for 1.0. | Avoid importing secrets/complexity. | Importer | State inspection | Later / Required | — |
| PD-REQ-040 | Updates | An outside-installed app MAY supply update artifacts only if safe validation and isolation are demonstrated. | Avoid trusting host installation state. | Updater/importer | Artifact provenance tests | Later / Required | Not a runtime dependency. |
| PD-REQ-041 | Storage | Protected storage MUST be isolated across host and protected apps, including native filesystem access; arbitrary host shared-storage passthrough is forbidden. | Prevent file disclosure. | Engine/storage/kernel | Java/native traversal probes | PR 4 / Gate | Explicit broker grants only. |
| PD-REQ-042 | Storage | Personas, policy, ledger, credentials, and sensitive state MUST be excluded from backup/transfer until validated. | Prevent out-of-boundary copies. | Storage/backup | OEM backup/transfer tests | Later / Required | Existing exclusions are not universal proof. |
| PD-REQ-043 | Removal | Removal MUST revoke grants/keys and clean logical state without claiming forensic erasure. | Honest deletion semantics. | Storage/key manager | State/key inspection | Later / Required | Remote state remains. |
| PD-REQ-044 | Launch | All mandatory validation MUST complete before any protected code, provider, or native initializer runs. | No pre-policy window. | Launcher/supervisor | Instrumented cold-start probes | PR 4 / Gate | Failure blocks launch. |
| PD-REQ-045 | Supervision | Supervisor health, authentication, recovery, and revocation MUST be fail-safe with bounded retries and no busy loop/restart storm. | Keep boundary dependable. | Supervisor | Fault/death/load tests | PR 4 / Gate | — |
| PD-REQ-046 | Entry routes | Launcher, shortcut, deep link, share, job, alarm, service, broadcast, push, notification action, and background entry MUST eventually be covered or blocked. | Alternate entry must not bypass validation. | Dispatcher/supervisor | Route matrix | Later / Required | Initial support may be narrow and explicit. |
| PD-REQ-047 | UX integration | Notifications and sharing MUST preserve originating protected-app identity where appropriate while visibly identifying Privacy Decoy. | Avoid source confusion. | UI/brokers | UX and spoofing tests | Later / Required | — |
| PD-REQ-048 | Boundary | Browser, authentication, share, keyboard, autofill, accessibility, and screen-capture handoffs MUST be explicit and documented. | These cross trust boundaries. | UX/policy | Handoff matrix and tests | Later / Required | External means not protected by Decoy. |
| PD-REQ-049 | UX safety | UI MUST clearly distinguish blocked, unsupported, unknown, and error states and protected versus outside-installed copies. | Users need actionable truth. | UX | Usability and copy review | Later / Required | Must explain remote-history limits. |
| PD-REQ-050 | App update | Protected-app updates MUST default to Ask and validate replacement, signature/lineage, splits, integrity, and coverage. | Updates can alter attack surface. | Updater/coverage | Upgrade fixture matrix | Later / Required | New versions may become unsupported. |
| PD-REQ-051 | App update | Update failure MUST preserve old package/state and MUST NOT reduce privacy; rollback MAY occur only when data-safe. | Compatibility never overrides privacy. | Updater/storage | Atomicity/rollback tests | Later / Required | — |
| PD-REQ-052 | Self-update | Self-update discovery MUST use published GitHub Releases only and ignore commits, branches, PRs, tag-only objects, drafts, and prereleases. | Define trusted release channel. | Updater | API fixture tests | Later / Required | Direct/GitHub distribution assumption. |
| PD-REQ-053 | Self-update | Update checks MUST NOT transmit persona/tracking data. | Minimize metadata. | Updater/network | Request inspection | Later / Required | — |
| PD-REQ-054 | Ledger | Privacy Access Ledger MUST be local, bounded, clearable, and metadata-only. | Accountability without a new sensitive corpus. | Ledger/store | Schema, bound, clear tests | Later / Required | No payloads, precise host data, or raw sensitive exceptions. |
| PD-REQ-055 | Ledger | Ledger failure MUST NOT open access, and ledger silence MUST NOT be represented as proof of non-access. | Logging is not enforcement. | Broker/ledger/UX | Failure injection | Later / Required | — |
| PD-REQ-056 | Resources | Queues, retries, caches, logs, and retained evidence MUST be bounded. | Resist exhaustion/privacy accumulation. | Every subsystem | Load/resource tests | Later / Required | No busy loops. |
| PD-REQ-057 | Testing | Validation MUST include positive, negative, adversarial, lifecycle, and failure tests using controlled probe apps and safe fixtures. | Ordinary apps are inadequate probes. | Test program | Reproducible suites | PR 4–6 / Gate | No real accounts/private data before gate. |
| PD-REQ-058 | Testing | Network claims MUST use independent packet observation; storage claims MUST use persistent-state observation; release claims MUST include release builds on non-rooted devices. | Avoid self-report circularity. | Independent observer | Captures/state snapshots/device records | PR 5–6 / Gate | Emulator-only evidence is insufficient. |
| PD-REQ-059 | Review | A later independent Android and native security review MUST precede production privacy claims. | TCB requires specialist scrutiny. | Project governance | Review report and remediation | Later / Required | Not replaced by internal tests. |
| PD-REQ-060 | Supply chain | Any engine MUST sit behind a documented replacement boundary with dependency, license, provenance, and vulnerability inventories. | Contain third-party and migration risk. | Engine adapter/build | SBOM/source/license audit | PR 4–6 / Gate | Runtime capability must not leak into management domain. |
| PD-REQ-061 | Distribution | Distribution-channel constraints and production-signing custody MUST be documented and tested; signing secrets MUST never enter git, prompts, logs, or artifacts. | Preserve authenticity. | Release process | Channel/policy and custody audit | Later / Required | Google Play feasibility remains open. |
| PD-REQ-062 | Repository | The clean-root rule MUST be preserved: Android material belongs under `android/`, project documentation under `docs/`, and new root files must be genuinely repository-wide. | Maintain navigable ownership. | Repository review | Tree/diff inspection | All / Required | Established by PR 2. |
| PD-REQ-063 | Feasibility | PR 6 MUST be a user decision STOP gate; work MUST stop, redesign, or mark unsupported when mandatory guarantees lack credible evidence. | Prevent momentum from becoming a false guarantee. | Governance | Signed decision against PR 4/5 evidence | PR 6 / Gate | No production engine is selected by this register. |

## Traceability and handoff

Each implementation change must cite requirement IDs and link tests and evidence.
PR 4 must answer execution without installation/re-signing; actual process/UID and
management boundaries; Binder/service/provider/package/host leakage; JNI/syscall,
`/proc`, `/sys`, filesystem/property leakage; early initialization; multiprocess;
stale capabilities; and pre-code fail-closed launch. PR 5 must identify every
traffic producer and establish attribution, external-VPN route verification,
route-change behavior, fail-closed networking, protocol/path coverage, and
independent packet evidence. PR 6 reviews those results and requires an explicit
user decision; it does not infer feasibility from application compatibility.
