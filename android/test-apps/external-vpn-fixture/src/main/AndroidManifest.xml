<?xml version="1.0" encoding="utf-8"?>
<!-- Separate disposable test application. This VpnService is NOT Privacy Decoy. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <queries><package android:name="com.privacydecoy.app" /></queries>
    <application android:allowBackup="false" android:label="External VPN test fixture"
        android:usesCleartextTraffic="false">
        <activity android:name=".FixtureController" android:exported="true" />
        <service android:name=".FixtureVpnService" android:exported="true"
            android:permission="android.permission.BIND_VPN_SERVICE"
            android:foregroundServiceType="specialUse">
            <property android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Disposable external VPN route research fixture" />
            <intent-filter><action android:name="android.net.VpnService" /></intent-filter>
        </service>
    </application>
</manifest>
